<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyu.Mapper.UserMapper">

    <insert id="CreateUser" parameterType="com.liyu.Pojo.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, phone)
        VALUES (#{username}, #{password}, #{phone})
    </insert>
    <select id="SearchUser" resultType="com.liyu.Pojo.User">
        select id,username,avatar from user where username like concat('%', #{name}, '%');
    </select>
    <select id="Login" resultType="com.liyu.Pojo.LoginInfo">
        select id,username,avatar from user where username = #{username} and password = #{password};
    </select>
</mapper>