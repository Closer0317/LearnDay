<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liyu.Mapper.NoteMapper">

    <insert id="CreateNote" parameterType="com.liyu.Pojo.Note">
        INSERT INTO note(user_id, title, sub_title, content, cover_image)
        VALUES (#{userId}, #{title}, #{subTitle}, #{content}, #{coverImage})
    </insert>
    <update id="ModifyNote">
        UPDATE note
        SET content = #{content}, update_time = NOW()
        WHERE id = #{id}
    </update>
    <delete id="DeleteNote">
        DELETE FROM note where id = #{id}
    </delete>
    <select id="QueryNoteById" resultType="com.liyu.Pojo.Note">
        SELECT title, sub_title, content, cover_image, create_time, update_time FROM note where id = #{id}
    </select>

</mapper>